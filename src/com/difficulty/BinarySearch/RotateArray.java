package com.difficulty.BinarySearch;

//第154题，此题依然可以直接for循环
public class RotateArray {
    public int findMin(int[] nums) {
        int min;
        int le=nums[0];
        int ri=nums[nums.length-1];
        return 1;
    }
}